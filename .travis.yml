branches:
  except:
    - gh-pages
language: perl6
sudo: false
perl6:
  - latest
#install:
#  - rakudobrew build-panda
#  - panda installdeps .
script:
  - export PREFIX=/home/travis/.rakudobrew/moar-nom/install/share/perl6/site
  - make test
  - make install
  - make clean
  - perl6 -e 'use Native::LibC <puts>; puts "ok."'
  - perl6 -e 'use Native::LibC <errno>; errno(42); say errno'
